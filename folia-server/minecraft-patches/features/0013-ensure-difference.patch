From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Loki Rautio <lokirautio@gmail.com>
Date: Thu, 19 Jun 2025 15:29:27 -0500
Subject: [PATCH] ensure difference


diff --git a/net/minecraft/world/entity/npc/AbstractVillager.java b/net/minecraft/world/entity/npc/AbstractVillager.java
index 2df839ac2b2c41e445b7bdbb4e411229bc106e8f..652211531ba3ba718992d6943602c0c09ec861a7 100644
--- a/net/minecraft/world/entity/npc/AbstractVillager.java
+++ b/net/minecraft/world/entity/npc/AbstractVillager.java
@@ -272,9 +272,10 @@ public abstract class AbstractVillager extends AgeableMob implements InventoryCa
 
     protected void addOffersFromItemListings(MerchantOffers givenMerchantOffers, VillagerTrades.ItemListing[] newTrades, int maxNumbers) {
         ArrayList<VillagerTrades.ItemListing> list = Lists.newArrayList(newTrades);
+        int originalTradeAmount = givenMerchantOffers.size();
         int i = 0;
 
-        while (i < maxNumbers && givenMerchantOffers.size() < maxNumbers && !list.isEmpty()) { // SMPOnline - Allow forcing trades
+        while (i < maxNumbers && (givenMerchantOffers.size() - originalTradeAmount) < maxNumbers && !list.isEmpty()) { // SMPOnline - Allow forcing trades
             MerchantOffer offer = list.remove(this.random.nextInt(list.size())).getOffer(this, this.random);
             if (offer != null) {
                 // CraftBukkit start
